# Core module CMakeLists.txt
# Sources are now added in parent src/CMakeLists.txt

# Link against required libraries if specific to this module
# (Linking is handled in the parent src/CMakeLists.txt for xenocomm_core) 